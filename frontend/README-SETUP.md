# PulsePoll Frontend

## Run
```powershell
npm install
copy .env.example .env
npm run dev
```

Default local API values:
```env
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080
```

For production use HTTPS/WSS URLs.
